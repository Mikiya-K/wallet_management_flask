# =============================================================================
# 钱包管理 Flask 应用环境变量配置示例
# =============================================================================
# 复制此文件为 .env 并根据你的环境修改相应的值
# 注意：.env 文件包含敏感信息，不应提交到版本控制系统

# =============================================================================
# 应用环境配置
# =============================================================================
# 应用运行环境：development, production
ENV=production

# =============================================================================
# 安全密钥配置 (必须设置)
# =============================================================================
# Flask 应用密钥，用于会话加密等，生产环境必须使用强密钥
SECRET_KEY=your-very-secure-secret-key-here-at-least-32-characters

# JWT 认证密钥，用于 JWT token 签名
JWT_SECRET_KEY=your-jwt-secret-key-here-different-from-secret-key

# =============================================================================
# 数据库配置
# =============================================================================
# 基础数据库连接 URL（用于注册服务动态创建不同数据库连接）
# 格式：postgresql://用户名:密码@主机:端口
DATABASE_URL=postgresql://username:password@localhost

# Flask 应用专用数据库连接 URL（包含具体数据库名）
# 格式：postgresql://用户名:密码@主机:端口/数据库名
FLASK_DATABASE_URL=postgresql://username:password@localhost/wallet_management

# Redis 缓存数据库连接 URL
REDIS_URL=redis://localhost:6379/0

# =============================================================================
# 钱包密码加密配置 (必须设置)
# =============================================================================
# 钱包主密钥，用于加密存储的钱包密码，必须是 Base64 编码的 32 字节密钥
# 生成方法：python -c "import base64, os; print(base64.b64encode(os.urandom(32)).decode())"
WALLET_MASTER_KEY=your-base64-encoded-32-byte-master-key-here

# PBKDF2 迭代次数，用于密钥派生，建议 100000 以上
WALLET_PBKDF2_ITERATIONS=100000

# =============================================================================
# Bittensor 网络配置
# =============================================================================
# Bittensor 网络类型：test, main
BITTENSOR_NETWORK=test

# Bittensor 钱包存储路径
BITTENSOR_WALLET_PATH=~/.bittensor/wallets

# 钱包密码（可选，用于自动解锁）
# BITTENSOR_WALLET_PASSWORD=your-wallet-password

# =============================================================================
# JWT 认证配置
# =============================================================================
# JWT 访问令牌过期时间（秒），默认 3600 (1小时)
JWT_ACCESS_EXPIRES=3600

# JWT 刷新令牌过期时间（秒），默认 86400 (1天)
JWT_REFRESH_EXPIRES=86400

# =============================================================================
# 安全配置
# =============================================================================
# 最大登录失败次数，超过后锁定账户
MAX_LOGIN_ATTEMPTS=5

# =============================================================================
# 日志配置
# =============================================================================
# 日志级别：DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=WARNING

# 日志文件路径
LOG_FILE_PATH=logs/app.log

# =============================================================================
# 示例配置值说明
# =============================================================================
# 以下是一些示例值，仅供参考，请根据实际情况修改：

# 示例数据库配置（PostgreSQL）：
# DATABASE_URL=postgresql://tao:btc2Moon@localhost
# FLASK_DATABASE_URL=postgresql://tao:btc2Moon@localhost/wallet_management

# 示例 Redis 配置：
# REDIS_URL=redis://localhost:6379/0

# 示例密钥生成（请生成你自己的密钥）：
# SECRET_KEY=HJ8vYxgGv33TcdwGgdBgNqLW6EPb8cHLu2DwubCPtS0=
# JWT_SECRET_KEY=different-key-from-secret-key-for-jwt-signing
# WALLET_MASTER_KEY=HJ8vYxgGv33TcdwGgdBgNqLW6EPb8cHLu2DwubCPtS0=

# =============================================================================
# 安全提醒
# =============================================================================
# 1. 生产环境必须使用强密钥，不要使用示例中的密钥
# 2. 定期轮换密钥，特别是 SECRET_KEY 和 JWT_SECRET_KEY
# 3. 确保数据库用户权限最小化
# 4. 使用 HTTPS 连接数据库（如果是远程数据库）
# 5. 定期备份 WALLET_MASTER_KEY，丢失将无法解密钱包密码
